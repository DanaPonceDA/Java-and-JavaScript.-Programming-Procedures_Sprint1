<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Item detail</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans text-gray-800">

    <div class="max-w-2xl mx-auto bg-white shadow-lg rounded-lg p-8">
      <h1 class="text-2xl font-bold text-gray-900 mb-4">{{item.nombre}}</h1>
      <p class="text-gray-700 mb-4">{{item.descripcion}}</p>

      <h2 class="text-xl text-gray-700 mb-4">
        Precio actual: $<span id="price-{{item.id}}">{{item.price}}</span>
      </h2>

      <a href="/offers/new?itemId={{item.id}}"
         class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
        Hacer una oferta
      </a>
    </div>

</body>
</html>
