<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Offer list</title>
    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans text-gray-800 min-h-screen">

    <div class="max-w-3xl mx-auto p-6">
        <h1 class="text-3xl font-bold mb-4 text-center">Offer List</h1>
        <div class="text-center mb-6">
            <a href="/offers/new" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition-colors">
                Create a new offer
            </a>

            <a href="/items"
                class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 transition-colors">
              ←Back to item list
            </a>

        </div>
        <hr class="border-gray-300 mb-6">

        {{#offers}}
        <ul class="space-y-4">
            {{#.}}
            <li class="bg-white p-4 rounded shadow hover:shadow-md transition-shadow">
                <div class="flex justify-between items-center">
                    <strong class="text-lg">{{itemName}}</strong>
                    <span class="text-gray-700 font-medium">${{offerPrice}}</span>
                </div>
                <p class="text-gray-600">Usuario: {{offerUser}}</p>
                <p class="text-gray-400 text-sm">Oferta ID: {{id}}</p>
            </li>
            {{/.}}
        </ul>
        {{/offers}}

        {{^offers}}
        <p class="text-center text-gray-500 mt-4">No offers yet.</p>
        {{/offers}}
    </div>

</body>
</html>
