<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>New offer</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans text-gray-800 min-h-screen flex items-center justify-center">

    <div class="bg-white p-8 rounded shadow-md w-full max-w-md">
        <h1 class="text-2xl font-bold mb-6 text-center">New offer</h1>

        <form action="/offers" method="post" class="flex flex-col gap-4">
            <div class="flex flex-col">
                <label for="itemId" class="mb-1 font-medium">Item:</label>
                <select name="itemId" id="itemId" required
                        class="border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400">
                    {{#items}}
                    <option value="{{id}}">{{nombre}} - ${{price}}</option>
                    {{/items}}
                </select>
            </div>

            <div class="flex flex-col">
                <label for="offerPrice" class="mb-1 font-medium">New offer:</label>
                <input type="number" name="offerPrice" id="offerPrice" step="0.01" required
                       class="border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400">
            </div>

            <div class="flex flex-col">
                <label for="offerUser" class="mb-1 font-medium">Email:</label>
                <input type="email" name="offerUser" id="offerUser" required
                       class="border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400">
            </div>

            <button type="submit"
                    class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition-colors mt-4">
                Send offer
            </button>
        </form>

        <a href="/offers" class="block mt-6 text-center text-blue-500 hover:text-blue-700 hover:underline">
            ← Back to offer list
        </a>
    </div>

</body>
</html>
